{
    "storages": ["SQL"],
    "default": "Transaction",
    "$schema": "http://json-schema.org/draft-07/schema",
    "id": "Transaction",
    "title": "Transaction",
    "type": "object",
    "properties": {"signedHash": {
        "storages": ["SQL"],
        "indexType": "INDEX_NOT_ANALYZE",
        "nullable": false,
        "minLength": 1,
        "description": "signed hash",
        "readOnly": true,
        "versionable": false,
        "id": "CE_Transaction_signedHash",
        "title": "Transaction.signedHash",
        "type": "string",
        "maxLength": 10000
    }},
    "required": ["signedHash"]
}